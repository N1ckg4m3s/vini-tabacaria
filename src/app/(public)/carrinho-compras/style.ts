import { flexCenter } from "@/styles/mixins";
import styled from "styled-components";

export const CartProdutsContainer = styled.ul`
    width: 100%;
    ${flexCenter};
    flex-direction:column;
    gap: 5px;
    list-style: none;
`
